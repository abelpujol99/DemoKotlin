<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@color/nav_bar">

        <ImageButton
            android:id="@+id/hero_detail_back_arrow"
            android:layout_width="@dimen/normal_button_size"
            android:layout_height="@dimen/normal_button_size"
            android:background="@drawable/ic_back_arrow_foreground">

        </ImageButton>

        <Space
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="1">
        </Space>

        <ImageButton
            android:id="@+id/hero_detail_delete_button"
            android:layout_width="@dimen/normal_button_size"
            android:layout_height="@dimen/normal_button_size"
            android:background="@drawable/ic_delete_icon_foreground">

        </ImageButton>

    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_normal">

            <TextView
                android:id="@+id/hero_detail_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="Hero Name"
                android:textAlignment="center"
                android:textSize="@dimen/text_title">

            </TextView>

            <TextView
                android:id="@+id/hero_detail_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="Hero Description"
                android:textAlignment="center"
                android:textSize="@dimen/text_regular"
                android:layout_marginTop="@dimen/padding_normal">

            </TextView>

        </LinearLayout>

    </ScrollView>


</LinearLayout>